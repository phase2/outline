import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { html } from 'lit';
import { OutlineSwatchSet, swatchSets } from './outline-swatch-set';

<Meta
  title="Utilities/Swatch Set"
  component="outline-swatch-set"
  argTypes={{
    set: {
      name: 'set',
      description: 'The color set.',
      options: swatchSets,
      control: { type: 'select' },
    },
  }}
/>

export const Template = ({ set }) => html`
  <outline-swatch-set set="${set}"> </outline-swatch-set>
`;

## Primary colors

<Canvas>
  <Story
    name="Brand Set"
    args={{
      set: 'brand',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
